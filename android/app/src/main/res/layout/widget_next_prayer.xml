<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/widget_next_prayer.xml
     Deen o Dunya — Next Prayer widget. Emerald card matching app identity.
     Kept simple and high-contrast so it reads on any wallpaper and on cheap screens. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_city"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Karachi"
                android:textColor="#A7F3D0"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/widget_prayer_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Maghrib"
                android:textColor="#FFFFFF"
                android:textSize="24sp"
                android:textStyle="bold"
                android:fontFamily="serif" />

            <TextView
                android:id="@+id/widget_prayer_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="7:42 PM"
                android:textColor="#FDE68A"
                android:textSize="18sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_ring"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="5/8"
            android:textColor="#A7F3D0"
            android:textSize="16sp"
            android:background="@drawable/widget_ring_bg"
            android:gravity="center"
            android:minWidth="44dp"
            android:minHeight="44dp"
            android:padding="8dp" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_prayer_in"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="in 2h 15m"
        android:textColor="#D1FAE5"
        android:textSize="13sp" />
</LinearLayout>
