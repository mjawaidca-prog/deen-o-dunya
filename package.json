{
  "name": "deen-o-dunya-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Deen o Dunya Planner PWA and mobile app package.",
  "scripts": {
    "check": "node scripts/check-store-readiness.js",
    "prepare:mobile": "node scripts/prepare-mobile-build.js",
    "cap:init": "npx cap init \"Deen o Dunya Planner\" com.deenodunya.planner --web-dir dist",
    "cap:add:android": "npx cap add android",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npm run prepare:mobile && npx cap sync",
    "android:open": "npx cap open android",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
