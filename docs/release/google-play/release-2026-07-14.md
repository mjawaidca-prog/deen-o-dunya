# Deen o Dunya Planner - Google Play Release Handoff

Generated: 2026-07-14
App: Deen o Dunya Planner
Package ID: `com.deenodunya.planner`
Release candidate: `versionName 1.3.0`, `versionCode 4`
Public app URL: https://mjawaidca-prog.github.io/deen-o-dunya/
Privacy policy URL: https://mjawaidca-prog.github.io/deen-o-dunya/privacy.html
Repository: `mjawaidca-prog/deen-o-dunya`

## Current Status

Use `versionCode 4` for the next Google Play upload. The Play Console history provided by the developer shows existing app bundle version codes 1, 2, and 3, with the latest uploaded version code 3 using version name `1.2.0`. Therefore the next valid upload must increment to version code 4. Version name `1.3.0` is the clean recommended label for this release.

## Verified Matches

| Item | Expected | Found | Status |
| --- | --- | --- | --- |
| App name | Deen o Dunya Planner | `capacitor.config.json`, `manifest.webmanifest` | Match |
| Android package ID | `com.deenodunya.planner` | `capacitor.config.json`; `android/app/build.gradle` namespace/applicationId | Match |
| Android version code | `4` | `android/app/build.gradle` versionCode `4` | Match |
| Android version name | `1.3.0` | `android/app/build.gradle` versionName `1.3.0` | Match |
| Package version | `1.3.0` | `package.json` version `1.3.0` | Match |
| SDK level | Play requires target API 35+ | `targetSdkVersion 36`, `compileSdkVersion 36` | Match |
| Min SDK | Project-defined | `minSdkVersion 24` | Found |
| Privacy policy source | Existing repo policy preserved | `privacy.html`, last updated June 2, 2026 | Found |

## AAB Build Instructions

Build the signed Google Play Android App Bundle from a clean local checkout after pulling the latest `main` branch.

```powershell
cd C:\Users\mjawa\deen-o-dunya
git pull origin main
npm install
npm run check
npm run cap:sync
cd android
.\gradlew.bat clean bundleRelease
```

Expected unsigned Gradle output path if building from command line:

```text
android\app\build\outputs\bundle\release\app-release.aab
```

For Google Play, create/upload a signed release AAB. The safest route is Android Studio:

1. Open the project with `npm run android:open`.
2. Choose Build > Generate Signed Bundle / APK.
3. Select Android App Bundle.
4. Select the existing upload keystore used for prior Play uploads.
5. Build the release bundle.
6. Upload the generated `.aab` to Google Play.

## Required Pre-Upload Check

Before uploading, inspect the generated AAB or Gradle output and confirm:

- `applicationId`: `com.deenodunya.planner`
- `versionCode`: `4`
- `versionName`: `1.3.0`
- `targetSdkVersion`: `36`

## Release Notes Draft

```text
New release for Deen o Dunya Planner.

- Added live Tafsir support in the Quran reading view.
- Added Tafsir language controls for English and Urdu.
- Added offline Tafsir download/caching support.
- Improved Quran and poster sharing controls with language selection and font-size handling.
- Improved app reliability and release configuration for Google Play.
```

## Remaining Release Checks

1. Confirm the signed release AAB was built after the latest repository changes.
2. Use the same upload keystore/Play App Signing setup as previous uploaded bundles.
3. Confirm the public privacy policy URL opens in a normal browser.
4. Confirm screenshots and feature graphic match the final version 1.3.0 build.
5. If AdMob remains enabled, ensure Play Console Ads/Data safety answers match the production build.

## Decision

The repository versioning is now set correctly for the next Google Play upload: `versionCode 4`, `versionName 1.3.0`. Do not upload any AAB that still shows `versionCode 7`, `versionName 1.5.1`, or any version code already used in Play Console.
